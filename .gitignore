# Engine-agnostic root. Each implementation has its own .gitignore:
#   godot-port/.gitignore   (.godot/, build/, logs)
#   raylib-port/.gitignore  (vendored raylib, build/, screenshots)

# OS / editor cruft
.DS_Store
*.swp
